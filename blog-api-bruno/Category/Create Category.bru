meta {
  name: Create Category
  type: http
  seq: 2
}

post {
  url: {{url}}/api/categories
  body: formUrlEncoded
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

body:form-urlencoded {
  name: New
}

settings {
  encodeUrl: true
}
